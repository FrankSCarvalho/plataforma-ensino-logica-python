"""Pacote de componentes visuais reutilizáveis da interface (Tarefa 09).

A identidade visual central e os elementos que se repetem em mais de uma
tela ficam aqui — evitando cores, espaçamentos e textos espalhados pelas
views:

    * ``theme``    — paleta, espaçamentos e tamanhos de texto;
    * ``widgets``  — elementos reutilizáveis (cabeçalho de estudo,
      cartão de conteúdo etc.).
"""